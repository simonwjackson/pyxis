import { SearchForm } from "./SearchForm.tsx"
export const name = "SearchForm"

export default function SearchFormPart() {
  return <SearchForm id="library-search" label="Search for an album" disabled={false} />
}
