import { IconButton } from "./IconButton.tsx"
export const name = "IconButton"

export default function IconButtonPart() {
  return <IconButton label="Play" icon="play" emphasis="primary" disabled={false} />
}
